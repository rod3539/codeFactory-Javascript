/**
 * Variable 선언하기
 * 1) var - 더이상 사용하지 않는다.
 * 2) let
 * 3) const
 */

var name = '코드팩토리';
console.log(name);

var age = 32;
console.log(age);

let ive = 32;
console.log(ive);
/**
 * let과 var로 선언시 값을 추후 변경 가능
 */

const newJeans = '뉴진스';
console.log(newJeans);

/**
 * 선언과 할당
 * 
 * 1) 변수를 선언하는 것
 * 2) 할당
 */